<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="softtech.office.dao.EmployeeInfoDao">
    <select id="getEmployee" parameterType="String" resultType="softtech.office.bean.EmployeeBean">
        SELECT employeeID,password FROM employee where mailAdress = #{mailAdress}
    </select>
    <!-- ログイン者IDを持っち、パスワードをresetする -->
	<select id="resetPassword"
		parameterType="softtech.office.bean.LoginBean"
		resultType="softtech.office.bean.EmployeeBean">
		SELECT employee.password
		FROM employee
		WHERE employee.employeeID = #{employeeID}
		AND employee.password = #{password}
	</select>
</mapper>