<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="softtech.office.dao.SararyInfoDao">
    <select id="getSalaryInfo" parameterType="softtech.office.bean.SalaryinfoSelectBean" resultType="softtech.office.bean.SalaryinfoBean">
        SELECT * FROM salaryinfo where employeeID = #{employeeID} and month = #{month}
    </select>
</mapper>